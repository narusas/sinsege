package net.narusas.aceauction;

import java.io.IOException;

import junit.framework.TestCase;

public class TableParserTest extends TestCase {
//	public void testCreateTables() {
//		String src = "<table><table><table>abc</table><table>def</table></table></table>";
//		TableParser parser = new TableParser();
//		List<Tag> tags = parser.parse(src);
//		assertEquals(8, tags.size());
//		List<Table> tables = parser.createTables(tags);
//		assertEquals(4, tables.size());
//		
//		List<Table> leafTables = parser.filterLeafTable(tables);
//		assertEquals(2, leafTables.size());
//		assertEquals("abc", leafTables.get(0).getText(src));
//		assertEquals("def", leafTables.get(1).getText(src));
//	}
	
	
	public void testRealHTML() throws IOException {
//		String src = NFile.getText(new File( "fixtures/speedy_search_result.html"));
//		TableParser parser = new TableParser();
//		List<Tag> tags = parser.parse(src);
////		System.out.println(tags);
//		List<Table> tables = parser.createTables(tags);
//		List<Table> leafTables = parser.filterLeafTable(tables);
//		for (Table table : leafTables) {
//			System.out.println(table.getText(src));
//			System.out.println("-------------------------------------------------------------------------");
//		}
	}
}
